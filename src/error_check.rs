use crate::command_funcs::check_distro;
use crate::command_funcs::command_input;
use crate::command_funcs::get_selection;

mod command_funcs;

fn check_selection_str(str_input: &str, str_type: String) {
    
}